<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lista de Productos</title>
    <style>
        body {
            font-family: 'Roboto', sans-serif;
            background-color: #1a1a2e;
            margin: 0;
            padding: 20px;
            color: #e0e0e0;
        }
        .container {
            max-width: 900px;
            margin: 0 auto;
            padding: 20px;
            background-color: #16213e;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        }
        h1 {
            text-align: center;
            color: #0f3460;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }
        th, td {
            padding: 10px;
            text-align: left;
            border-bottom: 1px solid #22273a;
        }
        th {
            background-color: #0f3460;
            color: white;
        }
        td {
            background-color: #22273a;
            color: #e0e0e0;
        }
        tr:hover {
            background-color: #2c3347;
            cursor: pointer;
        }
        .button-container {
            text-align: right;
            margin-bottom: 10px;
        }
        .crud-button {
            padding: 10px 15px;
            background-color: #e94560;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 16px;
            transition: background-color 0.3s ease;
        }
        .crud-button:hover {
            background-color: #ff6b6b;
        }
        .logout-link {
            display: block;
            text-align: center;
            margin-top: 20px;
            color: #e94560;
            text-decoration: none;
            transition: color 0.3s ease;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Lista de Productos</h1>

        <!-- Botón para ir a la interfaz CRUD -->
        <div class="button-container">
            <button class="crud-button" onclick="window.location.href='/producto/crud'">Administrar Productos</button>
        </div>

        <!-- Tabla de productos -->
        <table>
            <thead>
                <tr>
                    <th>Nombre</th>
                    <th>Descripción</th>
                    <th>Precio</th>
                    <th>Stock</th>
                </tr>
            </thead>
            <tbody>
                <% if (productos.length > 0) { %>
                    <% productos.forEach(producto => { %>
                        <tr onclick="window.location.href='/producto/<%= producto._id %>'">
                            <td><%= producto.Nombre %></td>
                            <td><%= producto.Descripcion %></td>
                            <td>$<%= producto.Precio.toFixed(2) %></td>
                            <td><%= producto.Stock %></td>
                        </tr>
                    <% }); %>
                <% } else { %>
                    <tr>
                        <td colspan="4" style="text-align: center;">No hay productos disponibles</td>
                    </tr>
                <% } %>
            </tbody>
        </table>
    </div>
    <a href="/logout" class="logout-link">Cerrar sesión</a>

</body>
</html>
